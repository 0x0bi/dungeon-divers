using DungeonDivers.Arena;
using DungeonDivers.Card;
using UnityEngine;

public class CardDeckUI : MonoBehaviour
{
    [SerializeField] private PlayerUnitController unitController;
    [SerializeField] private 
    public void OnPlayerChoice()
    {
        foreach (BaseCard baseCard in unitController.CurrentHand)
        {

        }
    }
}
